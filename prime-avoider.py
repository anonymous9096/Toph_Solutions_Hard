# Coded by SpiderX

x, y = 22, 34

def PrimeAvoider(x, y):
    increase = 1
    decrease = -1
    Summation_list = []

    x_list = list(str(x))
    y_list = list(str(y))

    loop = 0
    i = len(x)
    j = 0
    # 22 -> 32 -> 33 -> 24
    # while x == y:
        # while x_list[j] < y_list[j]:






if __name__ == "__main__":
    PrimeAvoider(x, y)
